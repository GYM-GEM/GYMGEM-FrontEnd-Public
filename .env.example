# Example Vite env for frontend (do NOT commit real secrets)
# Public client id used by client-side Google Identity Services
VITE_GOOGLE_CLIENT_ID=714832441360-5jejdlr712sj7fp3q0n32du29tbmu723.apps.googleusercontent.com

# NOTES:
# - Do NOT put your Google client secret in the frontend `.env` — it must stay on the server.
# - Restart the dev server after changing env vars.

# Backend (example) — these should go on your server-side environment (Django, etc.)
# DJANGO_GOOGLE_CLIENT_ID=714832441360-5jejdlr712sj7fp3q0n32du29tbmu723.apps.googleusercontent.com
# DJANGO_GOOGLE_CLIENT_SECRET=GOCSPX-Iv3D9lvZjzv3taCc-TMhzvSC4U
# DJANGO_GOOGLE_REDIRECT_URI=http://127.0.0.1:8000/accounts/google/login/callback

# Add this file as reference. Create a real `.env` in project root with the VITE_ variable set.
